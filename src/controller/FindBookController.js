const findNameBook = (name) => name;

const findReadingBook = (reading) => reading;

const findFinishedBook = (finished) => finished;

module.exports = { findNameBook, findReadingBook, findFinishedBook };
